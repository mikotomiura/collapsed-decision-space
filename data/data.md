# Data provenance

Frozen inputs live in `raw/` and are **never edited once placed**. Derived artefacts live in
`derived/`, are not tracked, and are regenerated by `repro.sh` on every run.

## raw/ — frozen inputs

| File | Origin (upstream path) | SHA-256 | Size | Retrieved | Licence |
|---|---|---|---|---|---|
| `bank_annotation.jsonl` | `experiments/20260710-m13-c-proper/artifacts/bank_annotation.jsonl` | `378b5423d5b9d927f884e4381c80dd2116ef9342b6f05a92a07b12cd17bdc6fc` | 682,130 bytes | 2026-09-12 | Research data generated by the author |
| `cproper-manifest.json` | `experiments/20260710-m13-c-proper/artifacts/manifest.json` | `99c8d9fb3bd0ffa11f010980c29e2b0cc2d7fd70170f63e3ef07b43ad5529bed` | 1,240 bytes | 2026-09-12 | Research data generated by the author |
| `cproper-verdict.json` | `experiments/20260710-m13-c-proper/artifacts/verdict.json` | `a3412e4a6cd843d5846c7769a1f025c6350055ed8f79b6e94c92b30e8ecde2ea` | 1,207 bytes | 2026-09-12 | Research data generated by the author |
| `es3-verdict-forensic.json` | `experiments/20260629-m13-es3-locomotion/data/raw/verdict-forensic.json` | `24c6d3ba479b17897ac99d32e91e60679a9cff5dc0896d49d0ba501120c385a2` | 11,743 bytes | 2026-09-12 | Research data generated by the author. The original lived in an untracked working directory upstream and was moved into a tracked path, byte for byte, on 2026-09-11. Upstream history therefore witnesses this record's **content** but not its **age** — see `analysis/freeze-provenance.json` (`provenance_kind = "relocation"`) and `manuscript/main.md` §12.5 |

> Recomputing a digest: `sha256sum data/raw/<file>` (POSIX) or
> `Get-FileHash -Algorithm SHA256 data/raw/<file>` (PowerShell).

### This table is the source, and it is machine-checked

**The table above is the single source for these digests.** Keeping a second, machine-readable copy
elsewhere would leave it ambiguous which one is authoritative, so there is no second copy;
`analysis/scripts/verify_data_hashes.py` parses this table directly (step 3 of `repro.sh`).

It checks five things:

1. the table parses as expected — **exactly four rows**, 64 hex digits, integer sizes. A parser that
   silently yields nothing would pass against any repository at all;
2. each file's SHA-256 and byte size match the row;
3. `data/raw/` holds **no file absent from the table**, so an input cannot be added unrecorded;
4. **cross-checks against pins written by the completed run itself**, which are independent of this
   file: `bank_annotation.jsonl` against `artifacts[...].sha256` in the run manifest, and
   `env/uv.lock` against `env_pins.uv_lock_sha256`;
5. **content-addressed comparison with the upstream blobs.** Checks 1–3 only establish that this
   file and the shipped bytes agree with each other — an integrity check closed inside the
   repository. Comparing against the upstream blob identifiers recorded in
   `analysis/freeze-provenance.json` is what establishes that the shipped bytes are the bytes
   registered upstream.

> Until 2026-09-12 this file recorded digests without verifying them. Recording and verifying are
> different acts, and the first does not license the language of the second. The five checks above
> close that gap.

## Prospective outputs in `raw/` (not frozen inputs)

The prospective arms of §6 write their verdicts into `data/raw/` as well, because that is where
step 13 of `repro.sh` reads them and `repro.sh` is sealed. They are **results, not frozen inputs**,
and the table above does not describe them:

- `data/raw/control-verdict.json` — the control arm (`qwen3:8b`), once it has run
- `data/raw/primary-verdict.json` — the primary arm (`llama3.1:8b`), once it has run

**There is no digest table for these, and that is deliberate.** A frozen input is pinned by a
SHA-256 recorded before the fact; a quantity that has not been measured has no digest to record,
and writing one in advance would be a claim about a result rather than a record of one. They also
cannot appear in `analysis/freeze-provenance.json`, which is sealed and predates them.

Both files are therefore excluded by name from the two frozen-input checks that would otherwise
reject them — "no unrecorded file in `data/raw/`" and "every shipped input has a provenance
entry". An exclusion is a hole, so it is bounded, and the excluded files are checked separately:

- the exclusion covers **exactly these two names**. Any other unrecorded file in `data/raw/` fails
  as it always did;
- the list above and `PROSPECTIVE_OUTPUTS` in `analysis/scripts/verify_data_hashes.py` are
  compared against each other in both directions, so this section cannot license a file the code
  does not, nor the reverse;
- each landed verdict must parse as a JSON object, and must **not be a byte-for-byte copy of a
  frozen input**. Copying `cproper-verdict.json` into both arms would pass the control gate and
  stop at R1 — a complete decision report, and a green run, out of a file that predates the
  prospective design. That is the shortest path from nothing to an apparently reported result,
  and it is closed;
- `verify_data_hashes.py` runs a mutation sweep over these guards on every invocation and reports
  what it caught, so "the exclusion is bounded" is measured rather than asserted.

### How a verdict gets here

Not by hand. The driver writes `run-verdict.json` into its own artefact directory, under a
different name from the one step 13 reads, and renaming files by hand is how the wrong file ends
up under the right name. Use:

```
python analysis/scripts/land_prospective_verdict.py --arm control --from <driver artefact dir>
```

It refuses a verdict the driver quarantined (`run-verdict.DEVIATION.json`, written when the sealed
seal check did not pass), refuses a copy of a frozen input, and will not overwrite an existing
landing without `--force`. Landing a verdict is not a substitute for having verified it: the
driver's `--verify` must have exited **0** first, where `2` means a bundle that is internally
consistent but not of the sealed size.

## derived/ — regenerated, not tracked

| File | Built from | Command |
|---|---|---|
| `verdict-table.md` | `raw/cproper-verdict.json`, `raw/cproper-manifest.json`, `raw/es3-verdict-forensic.json` | `python analysis/scripts/extract_verdict_table.py --out data/derived/verdict-table.md` |
| `power-curve.md` | `analysis/apparatus/.../bank_power.py` (assumed distributions only; reads no input file) | `python analysis/scripts/power_curve.py --out data/derived/power-curve.md` |

Both are written with LF endings regardless of platform, so the two legs of the CI can require them
to be byte-identical.

## Provenance of the apparatus (`analysis/apparatus/`)

- **Origin**: the import closure of the upstream `src/erre_sandbox/**`, 69 files, placed at
  `analysis/apparatus/erre_sandbox/<same relative path>` so that package-relative imports resolve.
- **Origin commit**: `589e881558f713b05312643cb842d0d924d1ce87`.
- **Verification**: `verify_threshold_freeze.py` recomputes the git blob identifier of all 69 files
  and requires each to equal the blob at that commit. "Copied byte for byte" is therefore a check,
  not an assurance.
- The scorer module was added on 2026-09-12, taking the closure from 68 files to 69.
  `raw/cproper-verdict.json` declares a `scorer_schema_version`, but the module that produced it was
  not shipped, so the central verdict could not be re-derived from this repository alone. No new
  transitive dependency came with it.

> One caveat about reading the vendored code: its docstrings contain references to upstream working
> directories that are **not shipped here**, so those paths lead nowhere for a reader. They cannot be
> edited away — the apparatus is shipped as the upstream bytes exactly, and changing one character
> would break the blob comparison that the freeze argument rests on. Any design decision a reader
> needs is restated in `manuscript/main.md`.

- **Use**: `PYTHONPATH=analysis/apparatus`.
- **Checking that it is self-contained** (the printed path must lie inside this repository):

  ```bash
  uv sync --frozen --no-install-project --project env
  PYTHONPATH=analysis/apparatus uv run --project env --no-sync python -c \
    "import erre_sandbox.evidence.es3_locomotion.verdict_report as v; print(v.__file__)"
  ```

  > **`--no-install-project` is required.** `env/pyproject.toml` is the upstream project definition,
  > kept verbatim next to `env/uv.lock` so that the lockfile the completed run used is preserved
  > unmodified. It declares `module-root = "src"`, and this repository has no `src/`, so without the
  > flag the command fails with `Expected a Python module at: env/src/erre_sandbox/__init__.py`.
  > The analysis scripts read the apparatus through `PYTHONPATH`, so the project itself never needs
  > to be installed.
  >
  > Until 2026-09-12 this file documented the command without the flag — and that command had never
  > once run. A documented command is only known to work when something actually runs it, which is
  > what `repro.sh` now does.

- **`env/uv.lock` is the lockfile the completed run used**: its SHA-256 equals the
  `uv_lock_sha256` pinned in `raw/cproper-manifest.json`
  (`9cc70f9dc5d61f6c74c08dee4dd73815993861022a80781a75ef5d873860c0f7`), and
  `verify_data_hashes.py` checks that equality on every run rather than asserting it.
- **Provenance of the frozen thresholds** is in `analysis/freeze-provenance.json`; what the
  verification does and does not establish is set out in `manuscript/main.md` §10.2.

## Recovering from an end-of-line normalisation (2026-09-12)

`raw/es3-verdict-forensic.json` was once committed with its carriage returns stripped, because the
author's global `core.autocrlf = input` normalised it at the moment it was first added
(11,743 → 11,401 bytes). The working tree still held the original bytes, so nothing surfaced until a
checkout replaced it.

This was not a harmless difference in line endings. The table above pins the original SHA-256 and
byte count, so publishing a normalised copy would have made the recorded digest disagree with the
shipped file — precisely what recording a digest is for.

The fix was to restore the file from the original, byte for byte, and to declare `data/raw/** -text`
and `env/uv.lock -text` in `.gitattributes` so that git never normalises them again. The restored
file measures 11,743 bytes with SHA-256 `24c6d3ba…`, matching the table.

## External data

External data, if any, must have its licence and redistribution terms stated here. Anything that
cannot be redistributed is not placed in `raw/`; `repro.sh` fetches it instead.

| Dataset | Source | Licence | Redistributable | How to obtain |
|---|---|---|---|---|
| `bank_records.jsonl` — the 17.7 MB per-draw record, SHA-256 `eec4b80db401c69b11e95c515385e72c0577ea9d9081e1e07148bca8d12352fb`, already pinned at `artifacts."bank_records.jsonl".sha256` in `raw/cproper-manifest.json` | Upstream `experiments/20260710-m13-c-proper/artifacts/bank_records.jsonl` | Research data generated by the author | Not shipped here, for size | Obtain from the upstream path and verify against the digest above |
