cff-version: 1.2.0
message: "If you cite this protocol or the accompanying code, please use the metadata below."
title: "When the power gate cannot fail: collapsed decision spaces defeat margin-and-power null reporting in LLM agents"
# This abstract must match the Abstract section of manuscript/main.md verbatim.
# analysis/scripts/check_claim_boundary.py compares them on every run, so editing
# one without the other fails repro.sh.
abstract: >-
  A common way to report a null in language-model evaluation pairs a materiality margin declared
  in advance with a power gate: the estimate must fall below the margin, and the design must have
  had the power to detect a shift of that size. We show that both guards can stop working at once,
  and in the same direction, when the categorical decision space partially collapses. In a
  completed measurement of an agent-internal channel over 4,800 draws, two of the five available
  zones are never produced in the channel-off condition that the power calculation takes as its
  base. The calculation builds its alternative by moving mass into the smallest cell, so a cell of
  probability exactly zero leaves the gate reporting 0.921 even at a hundredth of the declared
  margin of 0.10. The null floor of the distance statistic meanwhile stands at 0.027813, against
  an observed 0.038065. The channel is separately established as causal, separable and ablatable.
  To find out whether the failure mode belongs to the model or to the apparatus, the protocol then
  estimates the same quantity in a second model family; both arms have since been run once each,
  9,600 draws in total. The sealed rules stop at R4, apparatus validity: no context in the second
  family clears the per-context entropy floor, so the estimand is not measurable there and that
  question is not answered by this run. What the run does establish is that a validity gate
  evaluated ahead of the estimate reaches the case where the read-out cannot support the estimand,
  and does not reach the case this paper is about: applied to the completed run and to the control
  arm, neither of that gate's two conditions is met. Neither run that produced an estimate returned
  one its own permutation test rejected.
type: software
authors:
  - family-names: "Miura"
    given-names: "Mikoto"
    alias: "mikotomiura"
    orcid: "https://orcid.org/0009-0000-4196-0508"
    affiliation: "Independent Researcher"
# `license` mirrors the LICENSE files actually shipped in this repository.
#   Code (analysis/)         = Apache-2.0 OR MIT -> LICENSE / LICENSE-MIT
#   Manuscript (manuscript/) = CC-BY-4.0         -> LICENSE-CC-BY-4.0.txt
# CFF can only express a list of SPDX identifiers, not a per-part mapping.
# The exact mapping is in README.md, under Licence.
license:
  - Apache-2.0
  - MIT
  - CC-BY-4.0
repository-code: "https://github.com/mikotomiura/collapsed-decision-space"
version: "0.2.0-sealed-protocol"
date-released: "2026-09-13"
# doi: to be filled in once a DOI is minted. The key is omitted until then, because an
#      empty value would be rendered as-is by the GitHub citation widget.
# preferred-citation: to point at the peer-reviewed version once it exists. Omitted
#      until then, for the same reason.
keywords:
  - measurement validity
  - LLM agents
  - preregistration
  - reproducibility
  - statistical power
  - equivalence margin
