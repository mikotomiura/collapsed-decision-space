"""M13-ES1 SPDM (Spatial Path-Dependent Memory) evidence layer.

Deterministic, GPU/LLM-free probe for the foundational sub-claim of the
embodiment-substrate arc (M13): *does a same-base individual develop a
divergent retrieval landscape from spatial movement-history alone, against a
matched non-circular null battery?*

The package is **USE-only** over the frozen core (it never imports
``cognition``); its verdict thresholds in :mod:`constants` are pre-registered
*before* any result is seen and inherit the III-a ``live_carry`` house style
(scale-free ratio gate + degenerate-null floor + INCONCLUSIVE_LOW_POWER).

Claim boundary (parent design §1, Codex HIGH-2): this layer measures
**retrieval-landscape conformance** — a *necessary substrate*, not de-novo
cognitive divergence. A GO verdict means "eligible to proceed to ES-2
(replay / generation)", not "full-hypothesis sub-claim established".
"""

from __future__ import annotations
